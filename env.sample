# Copy to `.env` in this directory (or the directory you run Python from).
# Silkweb loads it automatically when you `import silkweb`.
# Replace placeholder values; never commit real secrets.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OLLAMA_HOST=http://127.0.0.1:11434
